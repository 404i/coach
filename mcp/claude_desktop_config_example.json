{
  "_comment": "Claude Desktop MCP configuration for Garmin AI Coach. Copy the relevant block into ~/Library/Application Support/Claude/claude_desktop_config.json",
  "_modes": "stdio = local machine only (no auth needed) | sse = local network (auth token required)",

  "mcpServers": {

    "_option_A_stdio_local": {
      "_comment": "Use when Claude Desktop runs on the SAME machine as the coach Docker stack",
      "command": "node",
      "args": ["/path/to/coach/mcp/coach-mcp-server.js"],
      "env": {
        "COACH_API_URL": "http://localhost:8088"
      }
    },

    "garmin-ai-coach": {
      "_comment": "Connects to the MCP server running on mithrandir.404i. Replace YOUR_MCP_AUTH_TOKEN with the value from ~/coach/.env on the server.",
      "url": "http://mithrandir.404i:3001/mcp",
      "headers": {
        "Authorization": "Bearer YOUR_MCP_AUTH_TOKEN_HERE"
      }
    }

  }
}
